# Environment Variables Configuration for Accessibility Booster

# Google Generative AI API Key
# Get your API key from: https://makersuite.google.com/app/apikey
NEXT_PUBLIC_GOOGLE_AI_API_KEY=your_api_key_here

# Alternative: You can also configure the API key via the Settings page in the app
# The app will first check this environment variable, then fallback to localStorage

# Chrome AI Writer/Rewriter API Configuration
# These are optional and will use defaults if not set
CHROME_AI_WRITER_REWRITER_ENDPOINT=https://generativelanguage.googleapis.com/v1beta
CHROME_AI_MODEL=gemini-1.5-flash
CHROME_AI_PROOFREADER_ENDPOINT=https://generativelanguage.googleapis.com/v1beta
CHROME_AI_TRANSLATOR_ENDPOINT=https://generativelanguage.googleapis.com/v1beta
CHROME_AI_PROMPT_ENDPOINT=https://generativelanguage.googleapis.com/v1beta
CHROME_AI_SUMMARIZER_ENDPOINT=https://generativelanguage.googleapis.com/v1beta

# Development Settings
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Instructions:
# 1. Copy this file to .env.local
# 2. Replace 'your_api_key_here' with your actual Google Generative AI API key
# 3. Restart your development server
